#include "main.h"

/**
 * _abs - computes the absolute value of a number n
 * @n: the int tobe checked
 * Return: the absolute value of an int
 */

int _abs(int n)
{
	if (n < 0)
		return (-n);
	return (n);
}
